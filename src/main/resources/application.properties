spring.application.name=JobBoardSpringBoot

# DB Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/Job-Board?useSSL=false
spring.datasource.username=root
spring.datasource.password=Kajal2003@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


#To add actuators
# Enable all endpoints
management.endpoints.web.exposure.include=*

# Optional: Change actuator base path (default is /actuator)
management.endpoints.web.base-path=/actuator

